import React from 'react';
import Navigation from './Navigation';

const Header = () => {
	return (
		<div className="header">
			<h1>React Movies</h1>
			<Navigation />
		</div>
	);
};

export default Header;
