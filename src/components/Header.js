import React from 'react';
import Navigation from './Navigation';

const Header = () => {
	return (
		<div className="header">
			<Navigation />
			<h1>React Movies</h1>
		</div>
	);
};

export default Header;
